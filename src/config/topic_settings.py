class TopicSettings:
    """
    Topic filter settings.
    """
    # Whether to use the fallback token whitelist when no tokens are extracted from the query.
    ALLOW_FALLBACK_TOKENS = False


topic_settings = TopicSettings()
